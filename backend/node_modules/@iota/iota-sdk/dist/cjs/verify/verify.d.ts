import type { PublicKey, SignatureScheme } from '../cryptography/index.js';
export declare function verifySignature(bytes: Uint8Array, signature: string): Promise<PublicKey>;
export declare function verifyPersonalMessageSignature(message: Uint8Array, signature: string): Promise<PublicKey>;
export declare function verifyTransactionSignature(transaction: Uint8Array, signature: string): Promise<PublicKey>;
export declare function publicKeyFromRawBytes(signatureScheme: SignatureScheme, bytes: Uint8Array): PublicKey;
export declare function publicKeyFromIotaBytes(publicKey: string | Uint8Array): PublicKey;
