import type { Codec } from '../../codec';
import { Signature } from './signature';
import { TypeSymbols } from '../../constants';
export declare class Credential {
    private readonly signatures;
    _type: TypeSymbols;
    constructor(signatures: Signature[]);
    static fromBytes(bytes: Uint8Array, codec: Codec): [Credential, Uint8Array];
    toJSON(): Signature[];
    static fromJSON(credentialsStrings: string[]): Credential;
    setSignature(index: number, signature: Uint8Array): void;
    getSignatures(): string[];
    toBytes(codec: any): Uint8Array;
}
//# sourceMappingURL=credential.d.ts.map