import { Bytes, Int } from '../../primitives';
import { Input, OutputOwnersList } from '../secp256k1';
import { TypeSymbols } from '../../constants';
export declare class MintOperation {
    private readonly input;
    private readonly groupId;
    private readonly payload;
    private readonly outputOwnerList;
    _type: TypeSymbols;
    constructor(input: Input, groupId: Int, payload: Bytes, outputOwnerList: OutputOwnersList);
    static fromBytes(bytes: Uint8Array): [MintOperation, Uint8Array];
    toBytes(codec: any): Uint8Array;
}
//# sourceMappingURL=mintOperation.d.ts.map