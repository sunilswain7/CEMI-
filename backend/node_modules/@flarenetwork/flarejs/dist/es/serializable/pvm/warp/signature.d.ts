import type { Codec } from '../../codec';
import { TypeSymbols } from '../../constants';
import { BlsSignature } from '../../fxs/common';
import { Bytes } from '../../primitives';
export declare class WarpSignature {
    readonly signers: Bytes;
    readonly signature: BlsSignature;
    _type: TypeSymbols;
    constructor(signers: Bytes, signature: BlsSignature);
    static fromBytes(bytes: Uint8Array, codec: Codec): [WarpSignature, Uint8Array];
    toBytes(codec: Codec): Uint8Array;
    numOfSigners(): number;
}
//# sourceMappingURL=signature.d.ts.map