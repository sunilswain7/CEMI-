import { DecodedSigningPayload, OptionsWithMeta, UnsignedTransaction } from '../../types';
/**
 * Parse the transaction information from a signing payload.
 *
 * @param signingPayload - The signing payload, in hex.
 * @param options - Runtime-specific data used for decoding the transaction.
 */
export declare function decodeSigningPayload(signingPayload: UnsignedTransaction | string, options: OptionsWithMeta): DecodedSigningPayload;
