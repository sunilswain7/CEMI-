import { BitGoBase, CoinConstructor, NamedCoinConstructor } from '@bitgo/sdk-core';
import { CantonTokenConfig } from '@bitgo/statics';
import { Canton } from './canton';
export declare class CantonToken extends Canton {
    readonly tokenConfig: CantonTokenConfig;
    constructor(bitgo: BitGoBase, tokenConfig: CantonTokenConfig);
    static createTokenConstructor(config: CantonTokenConfig): CoinConstructor;
    static createTokenConstructors(tokenConfig?: CantonTokenConfig[]): NamedCoinConstructor[];
    get name(): string;
    get coin(): string;
    get baseUrl(): string;
    get contractAddress(): string;
    get decimalPlaces(): number;
    getChain(): string;
    getBaseChain(): string;
    getFullName(): string;
    getBaseFactor(): number;
}
//# sourceMappingURL=cantonToken.d.ts.map