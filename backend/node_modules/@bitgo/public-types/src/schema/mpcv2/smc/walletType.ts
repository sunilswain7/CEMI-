import * as t from "io-ts";

export enum WalletTypeEnum {
  "tss" = "tss",
  "custodial" = "custodial",
  "cold" = "cold",
}

export const WalletType = t.keyof(WalletTypeEnum, "WalletType");

export type WalletType = t.TypeOf<typeof WalletType>;
