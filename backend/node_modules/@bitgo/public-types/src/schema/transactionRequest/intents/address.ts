import * as t from "io-ts";
import { optionalString, optionalUnknownObject } from "../../../utils";

export const Address = t.partial({
  address: optionalString,
  option: optionalUnknownObject,
});

export type Address = t.TypeOf<typeof Address>;
