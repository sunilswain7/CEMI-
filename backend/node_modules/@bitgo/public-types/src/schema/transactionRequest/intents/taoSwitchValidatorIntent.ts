import * as t from "io-ts";
import { BaseStakeIntent } from "./baseStakeIntent";
import { SubstrateAccountBaseBuildOptions } from "./substrateAccountBaseBuildOptions";
import { intentTypes } from "./intentType";

/**
 * @title Tao Switch Validator Intent
 */
export const TaoSwitchValidatorIntent = t.intersection([
  BaseStakeIntent,
  SubstrateAccountBaseBuildOptions,
  t.type({
    intentType: intentTypes.switchValidator,
    originHotkey: t.string,
    destinationHotkey: t.string,
    originNetuid: t.string,
    destinationNetuid: t.string,
    alphaAmount: t.string,
  }),
]);

export type TaoSwitchValidatorIntent = t.TypeOf<
  typeof TaoSwitchValidatorIntent
>;
