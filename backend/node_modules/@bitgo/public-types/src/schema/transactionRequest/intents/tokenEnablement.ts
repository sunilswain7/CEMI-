import * as t from "io-ts";
import { optionalString } from "../../../utils";

export const TokenEnablement = t.intersection([
  t.type({
    name: t.string,
  }),
  t.partial({
    address: optionalString,
  }),
]);

export type TokenEnablement = t.TypeOf<typeof TokenEnablement>;
