import * as t from "io-ts";
import { optionalNumber, optionalString } from "../../../utils";

export const HopParams = t.type({
  paymentId: optionalString,
  userReqSig: optionalString,
  gasPriceMax: optionalNumber,
});

export type HopParams = t.TypeOf<typeof HopParams>;
