import * as t from "io-ts";
import { optionalString } from "../../../utils";

export const DotProxy = t.partial({
  name: optionalString,
  addressString: optionalString,
  asset: optionalString,
  amountString: optionalString,
});

export type DotProxy = t.TypeOf<typeof DotProxy>;
