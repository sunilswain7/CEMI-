import * as t from "io-ts";
import { SignMessageIntent } from "./signMessageIntent";

/**
 * @title MMI Sign Message Intent
 */
export const MmiSignMessageIntent = t.intersection([
  SignMessageIntent,
  t.type({
    custodianMessageId: t.string,
  }),
]);

export type MmiSignMessageIntent = t.TypeOf<typeof MmiSignMessageIntent>;
