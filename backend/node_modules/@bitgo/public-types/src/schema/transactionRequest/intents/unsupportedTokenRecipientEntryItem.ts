import * as t from "io-ts";
import { optionalNumber, optionalString } from "../../../utils";

export const UnsupportedTokenRecipientEntryItem = t.partial({
  tokenContractAddress: optionalString,
  decimals: optionalNumber,
});

export type UnsupportedTokenRecipientEntryItem = t.TypeOf<
  typeof UnsupportedTokenRecipientEntryItem
>;
