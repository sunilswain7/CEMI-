import * as t from "io-ts";

export const feeTypes = {
  base: t.literal("base"),
  max: t.literal("max"),
  tip: t.literal("tip"),
};

export const FeeType = t.keyof(feeTypes);

export type FeeType = t.TypeOf<typeof FeeType>;
