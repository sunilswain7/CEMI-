import * as t from "io-ts";

export const feeUnits = {
  baseUnit: t.literal("baseUnit"),
  cpu: t.literal("cpu"),
  ram: t.literal("ram"),
};

export const FeeUnit = t.keyof(feeUnits);

export type FeeUnit = t.TypeOf<typeof FeeUnit>;
