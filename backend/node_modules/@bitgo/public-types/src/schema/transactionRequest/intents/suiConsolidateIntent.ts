import * as t from "io-ts";
import { SuiBuildOptions } from "./suiBuildOptions";
import { AccountBaseConsolidateIntent } from "./accountBaseConsolidateIntent";

/**
 * @title SUI Consolidate Intent
 */
export const SuiConsolidateIntent = t.intersection([
  AccountBaseConsolidateIntent,
  SuiBuildOptions,
]);

export type SuiConsolidateIntent = t.TypeOf<typeof SuiConsolidateIntent>;
