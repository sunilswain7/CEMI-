import * as t from "io-ts";

export const SupportedTokenRecipientEntryItem = t.type({
  tokenName: t.string,
});

export type SupportedTokenRecipientEntryItem = t.TypeOf<
  typeof SupportedTokenRecipientEntryItem
>;
