import { BitGoBase, CoinConstructor, NamedCoinConstructor } from '@bitgo/sdk-core';
import { JettonTokenConfig } from '@bitgo/statics';
import { Ton } from './ton';
export declare class JettonToken extends Ton {
    readonly tokenConfig: JettonTokenConfig;
    constructor(bitgo: BitGoBase, tokenConfig: JettonTokenConfig);
    static createTokenConstructor(config: JettonTokenConfig): CoinConstructor;
    static createTokenConstructors(tokenConfig?: JettonTokenConfig[]): NamedCoinConstructor[];
    get name(): string;
    get coin(): string;
    get network(): string;
    get contractAddress(): string;
    get decimalPlaces(): number;
    getChain(): string;
    getBaseChain(): string;
    getFullName(): string;
    getBaseFactor(): number;
}
//# sourceMappingURL=jettonToken.d.ts.map