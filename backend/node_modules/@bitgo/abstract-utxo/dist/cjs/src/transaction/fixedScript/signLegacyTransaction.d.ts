import * as utxolib from '@bitgo/utxo-lib';
import { bip32 } from '@bitgo/wasm-utxo';
import { UtxoCoinName } from '../../names';
import type { Unspent } from '../../unspent';
type RootWalletKeys = utxolib.bitgo.RootWalletKeys;
/**
 * Sign all inputs of a wallet transaction and verify signatures after signing.
 * Collects and logs signing errors and verification errors, throws error in the end if any of them
 * failed.
 *
 * @param transaction - wallet transaction (builder) to be signed
 * @param unspents - transaction unspents
 * @param walletSigner - signing parameters
 * @param coinName - coin name for network-specific logic
 * @param isLastSignature - Returns full-signed transaction when true. Builds half-signed when false.
 * @param replayProtectionAddresses - List of replay protection addresses to skip signing
 */
export declare function signAndVerifyWalletTransaction<TNumber extends number | bigint>(transaction: utxolib.bitgo.UtxoTransaction<TNumber> | utxolib.bitgo.UtxoTransactionBuilder<TNumber>, unspents: Unspent<TNumber>[], walletSigner: utxolib.bitgo.WalletUnspentSigner<RootWalletKeys>, coinName: UtxoCoinName, { isLastSignature, replayProtectionAddresses, }: {
    isLastSignature: boolean;
    replayProtectionAddresses?: string[];
}): utxolib.bitgo.UtxoTransaction<TNumber>;
export declare function signLegacyTransaction<TNumber extends number | bigint>(tx: utxolib.bitgo.UtxoTransaction<TNumber>, signerKeychain: bip32.BIP32Interface | undefined, coinName: UtxoCoinName, params: {
    isLastSignature: boolean;
    signingStep: 'signerNonce' | 'cosignerNonce' | 'signerSignature' | undefined;
    txInfo: {
        unspents?: Unspent<TNumber>[];
    } | undefined;
    pubs: string[] | undefined;
    cosignerPub: string | undefined;
}): utxolib.bitgo.UtxoTransaction<TNumber>;
export {};
//# sourceMappingURL=signLegacyTransaction.d.ts.map