import { CreateAddressFormat, Triple } from '@bitgo/sdk-core';
import { fixedScriptWallet } from '@bitgo/wasm-utxo';
import { UtxoCoinName } from '../names';
type ChainCode = fixedScriptWallet.ChainCode;
export interface FixedScriptAddressCoinSpecific {
    outputScript?: string;
}
export interface GenerateAddressOptions {
    chain?: number;
    index?: number;
    segwit?: boolean;
    bech32?: boolean;
}
interface GenerateFixedScriptAddressOptions extends GenerateAddressOptions {
    format?: CreateAddressFormat;
    keychains: {
        pub: string;
    }[];
}
export declare function generateAddressWithChainAndIndex(coinName: UtxoCoinName, keychains: fixedScriptWallet.WalletKeysArg | Triple<string>, chain: ChainCode, index: number, format: CreateAddressFormat | undefined): string;
/**
 * Generate an address for a wallet based on a set of configurations
 * @param params.keychains   {[object]} Array of objects with xpubs
 * @param params.threshold   {number}   Minimum number of signatures
 * @param params.chain       {number}   Derivation chain (see https://github.com/BitGo/unspents/blob/master/src/codes.ts for
 *                                                 the corresponding address type of a given chain code)
 * @param params.index       {number}   Derivation index
 * @param params.segwit      {boolean}  Deprecated
 * @param params.bech32      {boolean}  Deprecated
 * @returns {string} The generated address
 */
export declare function generateAddress(coinName: UtxoCoinName, params: GenerateFixedScriptAddressOptions): string;
type Keychain = {
    pub: string;
};
export declare function assertFixedScriptWalletAddress(coinName: UtxoCoinName, { chain, index, keychains, format, address, }: {
    chain: number | undefined;
    index: number;
    keychains: Keychain[];
    format: CreateAddressFormat;
    address: string;
}): void;
export {};
//# sourceMappingURL=fixedScript.d.ts.map