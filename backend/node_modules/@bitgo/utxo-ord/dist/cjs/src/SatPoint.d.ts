export type SatPoint = `${string}:${number}:${bigint}`;
/**
 * Parse an output ID (txid:vout) into its components.
 */
export declare function parseOutputId(outputId: string): {
    txid: string;
    vout: number;
};
export declare function parseSatPoint(p: SatPoint): {
    txid: string;
    vout: number;
    offset: bigint;
};
export declare function formatSatPoint(p: {
    txid: string;
    vout: number;
    offset: bigint;
}): SatPoint;
export declare function isSatPoint(v: string): v is SatPoint;
//# sourceMappingURL=SatPoint.d.ts.map