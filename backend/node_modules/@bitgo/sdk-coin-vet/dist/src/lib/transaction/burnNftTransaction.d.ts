import { BaseCoin as CoinConfig } from '@bitgo/statics';
import { Transaction as VetTransaction } from '@vechain/sdk-core';
import { Transaction } from './transaction';
import { VetTransactionData } from '../iface';
export declare class BurnNftTransaction extends Transaction {
    private _tokenId;
    private _stakingContractAddress;
    constructor(_coinConfig: Readonly<CoinConfig>);
    get tokenId(): string;
    set tokenId(id: string);
    get stakingContractAddress(): string;
    set stakingContractAddress(address: string);
    /** @inheritdoc */
    build(): Promise<void>;
    /** @inheritdoc */
    buildClauses(): void;
    /**
     * Generates the transaction data for burning NFT by encoding the burn method call.
     *
     * @private
     * @returns {string} The encoded transaction data as a hex string
     */
    private getBurnNftData;
    /** @inheritdoc */
    toJson(): VetTransactionData;
    /** @inheritdoc */
    fromDeserializedSignedTransaction(signedTx: VetTransaction): void;
}
//# sourceMappingURL=burnNftTransaction.d.ts.map