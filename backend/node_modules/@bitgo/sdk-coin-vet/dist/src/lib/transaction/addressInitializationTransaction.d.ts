import { BaseCoin as CoinConfig } from '@bitgo/statics';
import { Transaction as VetTransaction } from '@vechain/sdk-core';
import { Transaction } from './transaction';
import { VetTransactionData } from '../iface';
export declare class AddressInitializationTransaction extends Transaction {
    private _baseAddress;
    private _feeAddress;
    private _salt;
    private _initCode;
    private _deployedAddress;
    constructor(_coinConfig: Readonly<CoinConfig>);
    get baseAddress(): string;
    set baseAddress(address: string);
    get feeAddress(): string;
    set feeAddress(address: string);
    get salt(): string;
    set salt(salt: string);
    get initCode(): string;
    set initCode(initCode: string);
    get deployedAddress(): string;
    set deployedAddress(address: string);
    /** @inheritdoc */
    build(): Promise<void>;
    /** @inheritdoc */
    buildClauses(): void;
    /** @inheritdoc */
    toJson(): VetTransactionData;
    /** @inheritdoc */
    fromDeserializedSignedTransaction(signedTx: VetTransaction): void;
}
//# sourceMappingURL=addressInitializationTransaction.d.ts.map