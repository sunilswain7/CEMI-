import { BaseCoin as CoinConfig } from '@bitgo/statics';
import { Transaction as VetTransaction } from '@vechain/sdk-core';
import { Transaction } from './transaction';
import { VetTransactionData } from '../iface';
export declare class FlushTokenTransaction extends Transaction {
    private _forwarderVersion;
    private _tokenAddress;
    constructor(_coinConfig: Readonly<CoinConfig>);
    get forwarderVersion(): number;
    set forwarderVersion(forwarderVersion: number);
    get tokenAddress(): string;
    set tokenAddress(address: string);
    /** @inheritdoc */
    buildClauses(): void;
    /** @inheritdoc */
    toJson(): VetTransactionData;
    /** @inheritdoc */
    fromDeserializedSignedTransaction(signedTx: VetTransaction): void;
}
//# sourceMappingURL=flushTokenTransaction.d.ts.map