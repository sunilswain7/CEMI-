import { AuditDecryptedKeyParams, BaseCoin, BaseTransaction, BitGoBase, KeyPair, MPCAlgorithm, MultisigType, ParsedTransaction, ParseTransactionOptions, PopulatedIntent, PrebuildTransactionWithIntentOptions, SignedTransaction, SignTransactionOptions, TssVerifyAddressOptions, VerifyTransactionOptions } from '@bitgo/sdk-core';
import { BaseCoin as StaticsBaseCoin } from '@bitgo/statics';
import { TransactionBuilderFactory } from './lib';
import { ExplainTransactionOptions } from './lib/types';
import { AptTransactionExplanation } from './lib/iface';
export interface AptParseTransactionOptions extends ParseTransactionOptions {
    txHex: string;
}
export declare class Apt extends BaseCoin {
    protected readonly _staticsCoin: Readonly<StaticsBaseCoin>;
    protected constructor(bitgo: BitGoBase, staticsCoin?: Readonly<StaticsBaseCoin>);
    static createInstance(bitgo: BitGoBase, staticsCoin?: Readonly<StaticsBaseCoin>): BaseCoin;
    /**
     * Factor between the coin's base unit and its smallest subdivison
     */
    getBaseFactor(): number;
    getChain(): string;
    getFamily(): string;
    getFullName(): string;
    /** @inheritDoc */
    supportsTss(): boolean;
    /** inherited doc */
    getDefaultMultisigType(): MultisigType;
    getMPCAlgorithm(): MPCAlgorithm;
    allowsAccountConsolidations(): boolean;
    verifyTransaction(params: VerifyTransactionOptions): Promise<boolean>;
    /**
     * Verify that an address belongs to this wallet.
     *
     * @param params - Verification parameters including address, keychains, and index
     * @returns True if address belongs to wallet
     * @throws UnexpectedAddressError if address doesn't match derived address
     */
    isWalletAddress(params: TssVerifyAddressOptions): Promise<boolean>;
    parseTransaction(params: AptParseTransactionOptions): Promise<ParsedTransaction>;
    /**
     * Explain a Apt transaction
     * @param params
     */
    explainTransaction(params: ExplainTransactionOptions): Promise<AptTransactionExplanation | undefined>;
    generateKeyPair(seed?: Buffer): KeyPair;
    isValidPub(pub: string): boolean;
    isValidAddress(address: string): boolean;
    signTransaction(params: SignTransactionOptions): Promise<SignedTransaction>;
    protected getTxBuilderFactory(): TransactionBuilderFactory;
    protected rebuildTransaction(txHex: string): Promise<BaseTransaction>;
    /** @inheritDoc */
    auditDecryptedKey({ multiSigType, prv, publicKey }: AuditDecryptedKeyParams): void;
    setCoinSpecificFieldsInIntent(intent: PopulatedIntent, params: PrebuildTransactionWithIntentOptions): void;
}
//# sourceMappingURL=apt.d.ts.map